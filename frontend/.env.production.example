# Set this in the Vercel dashboard under Project → Settings → Environment Variables.
# Value is your Render backend URL (copy after first deploy).
VITE_API_URL=https://pharmalens-api.onrender.com/api/v1
